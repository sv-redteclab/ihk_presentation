# Abschlusspräsentation Sven Vaupel

Finaler Foliensatz: `presentation.pdf`

Run `./init.sh` to install dependencies.
Run `npm start` to start presentation (runs on localhost:8000).
Press `s` to show speaker notes/
